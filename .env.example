DATABASE_URL="file:./dev.db"
# Production only. 32 random bytes as 64 hex chars: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Never change after distributing client software (their embedded public key would stop matching).
# LICENSE_SIGNING_SEED=
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=
